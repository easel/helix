# Parking Lot Dependencies
artifact: parking-lot
prefix: PL

inputs:
  required: []
  optional:
    - artifact: prd
      phase: "01-frame"
      relationship: "captures deferred and future items"

outputs:
  produces:
    - artifact: parked-artifacts
      relationship: "tracks deferred work across phases"

completion:
  automated_checks:
    - type: file_exists
  manual_checks:
    - "Each entry includes Source and Revisit Trigger"
